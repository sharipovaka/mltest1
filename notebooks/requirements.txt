# Лабораторный практикум по машинному обучению (каф. ФН1)
# Установка:  pip install -r requirements.txt
#
# Практикум сознательно обходится базовым научным стеком: xgboost, lightgbm,
# cvxopt, shap и torch НЕ требуются -- всё реализуется на numpy/scipy/sklearn.
# Это снимает проблемы с установкой и делает работы воспроизводимыми.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
jupyter>=1.0
notebook>=7.0

# Необязательно (упоминаются в работах как альтернативы, но не нужны):
# xgboost>=2.0
# lightgbm>=4.0
